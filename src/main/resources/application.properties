# ======================
# Database
# ======================
app.db.type=h2
app.db.path=./data/memo

# H2 console
app.h2.console.enabled=true
app.h2.console.port=8084
app.h2.console.allow-others=false

# ======================
# Runtime-managed options
# ======================
# Note: Most UI and scheduler settings are now managed at runtime via
# View -> Preferences... and persisted to data/prefs.properties. Keys such as
# app.ui.theme, app.ui.mode, app.window.*, app.study.* (scheduler/batch/snooze)
# no longer live here to avoid duplication.
